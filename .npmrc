# Canvas binary mirror for faster download
canvas_binary_host_mirror=https://registry.npmmirror.com/-/binary/canvas/

# Use Chinese npm registry for faster package download
registry=https://registry.npmmirror.com/

# Prevent canvas from building from source if binary is available
canvas_pre_gyp_verbose=true